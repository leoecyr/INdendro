// Variables requiring visibility across modules
int thermValue = 0;  // variable to store the value coming from the sensor

int loadSampleDiscard = 3;  // count of samples to discard before averaging
int loadSamples = 10;  // Count of samples averaged
